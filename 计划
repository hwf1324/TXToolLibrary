tar,gzip,bzip,zip
linux内核(等开始调用内核的时候才介绍)，启动工具
arch和pacman
ubuntu和dpkg,apt
emacs
clear,tree
diff

#Linux拾遗：

linux内核，linux-docs
clear
tree
diff

顺便整理一下收藏，把进程管理，用户管理，系统必须文件管理等内容放到Linux系统中（htop，tree，trash，sudo，su等仍旧不算系统的一部分）

以后要介绍Windows等系统

#归档压缩
tar，gzip系列
7zip

#包管理工具和发行版

Ubuntu/Debian，dpkg，apt
Arch，pacman

#GUI系列：

X11
桌面系统
虚拟终端机
顺便整理一下网站收藏





开始写第一个关于内核的命令的时候介绍linux内核(以及安装linux-docs)


介绍Arch的时候

把每个没有介绍到但是需要的包说一下
linux内核
boot，各种lib等

然后说一下Arch的包管理系统

对于库,写程序用到的时候再介绍
对于手册文档,需要查询的时候再介绍

# 操作系统内核Linux
1. Linux并不是一个完整的系统。它只是一个实现操作系统基本功能的内核。内核配以一系列其他的库和软件才能成为一个完整的操作系统。事实上，我们经常用Linux来指代一系列由Linux内核和GNU计划软件组成的操作系统。
2. GNU计划的目标是创建一套完全自由的操作系统。但这个系统的内核Hurd历经多年开发都没有稳定。 因此，反倒是GNU计划的其他软件配以同为开源软件的Linux形成的所谓GNU/Linux操作系统取得了成功。
3. 

中心法则
以笔记本电脑和其上操作系统ArchLinux或Windows作为核心
逐步扩展介绍各种软硬件。

## 组成
## 依赖
## 定位
## 简介
## 相关与替代
## 后续推荐

一定要先看一看包是单个包还是包组
说明：组成，依赖都以Arch Linux包管理系统为准。

未介绍的被依赖包
* readline


下一步介绍:

pylint

bash(包括自动补全拓展),说一下可以用Python取代逻辑功能,也可以用Lua取代
革奴utilcore工具包(分成几个模块介绍)(vim直接引用,作为一个简单的文本编辑器介绍.不深入)
python通用系统库(取代bash的逻辑功能)

准备中: 

pandoc(markdown,tex)
pacman和arch ,debian/ubuntu 和apt/dpkg
torch,pytorch

html,xml,sgml

VScode

emacs,elisp

SQL(数据库)

Jupyter
JupyterLab

skimage

考虑中:


pyqt (pyqt5)
opencv-python
